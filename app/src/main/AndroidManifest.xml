<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lostdotcom.notifind">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".how_to_use"></activity>
        <activity android:name=".Viewing.AdminViewingActivity" />
        <activity android:name=".Activities.ReportEditsActivity" />
        <activity android:name=".Viewing.ReportProfile" />
        <activity android:name=".Activities.AdminCreationActivity" />
        <activity android:name=".Viewing.ReportViewingActivity" />
        <activity android:name=".Activities.AboutActivity" />
        <activity android:name=".Activities.SettingsActivity" />
        <activity android:name=".Activities.ReportCreationActivity" />
        <activity android:name=".LoginSystem.ForgotPassword" />
        <activity android:name=".LoginSystem.SignUpActivity" />
        <activity android:name=".LoginSystem.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>